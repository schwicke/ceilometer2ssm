usrlibexecdir =  $(prefix)/usr/libexec
usrlibexec_SCRIPTS = ceilometer2ssm cloudaccounting
crondir = $(prefix)/etc/cron.d
cron_DATA = cloudaccounting.cron
logrotdir = $(prefix)/etc/logrotate.d
logrot_DATA = cloudaccountinglogs
